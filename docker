#! /bin/bash
OS=$(uname -m)
if [[ ${OS} == "x86_64" ]]; then
bash <(curl https://raw.githubusercontent.com/mslxi/docker/main/docker)
fi

if [[ ${OS} == "aarch64" ]]; then
bash <(curl https://raw.githubusercontent.com/mslxi/docker/main/docker-arm)
fi
